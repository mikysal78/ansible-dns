// TSIG key per trasferimenti di zona (AXFR/IXFR)
// Generato da Ansible — NON MODIFICARE MANUALMENTE
// Protetto: owner root, group bind, mode 0640

key "{{ dns_tsig_key_name }}" {
    algorithm {{ dns_tsig_algorithm }};
    secret "{{ vault_tsig_secret }}";
};
