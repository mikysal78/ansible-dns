# Dipendenze Python per CI/CD e sviluppo locale
# Usato da actions/setup-python per la cache pip
ansible-core>=2.16
ansible-lint>=24.0
yamllint>=1.35
molecule>=6.0
molecule-plugins[docker]>=23.5
docker>=7.0
jmespath>=1.0
